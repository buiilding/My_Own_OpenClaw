websockets==12.0
pylint==3.0.3
pytest==8.1.1
pytest-asyncio==0.23.6
black==24.3.0
pyyaml==6.0.1
pylint-pydantic==0.3.0
pydantic==2.8.2
litellm==1.81.16
httpx==0.27.0
rapidocr==3.4.5
pillow==12.0.0
transformers==4.57.3
accelerate==1.12.0
einops==0.8.1
timm==1.0.22
# python-magic-bin
onnxruntime-gpu
# onnxruntime
# openwakeword==0.6.0  # Not used in backend - only needed in frontend wakeword_service.py
#flash-attn  # Optional: improves performance but may have installation issues on Windows
numpy<2.3.0,>=2

# System integration
psutil==7.1.3
pyperclip==1.11.0
pexpect

# Web Framework
fastapi==0.121.3
uvicorn==0.38.0

# Dependency Injection
dependency-injector==4.41.0

# Embeddings (backend only generates embeddings, storage handled by frontend)
sentence-transformers==5.2.0  # Required for generating text embeddings
# Note: numpy is already listed above in "Deep Learning & Vision Models" section

# Provider-specific SDKs required by LiteLLM
google-auth==2.30.0
openai==1.99.5
anthropic==0.72.0
mistralai==0.4.1

# TTS dependencies
piper-tts==1.3.0

# File locking for thread-safe config writes
filelock==3.15.1
python-multipart
