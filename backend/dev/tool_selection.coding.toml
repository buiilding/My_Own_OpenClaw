# Coding tools profile
enabled = true
mode = "allowlist"
tools = [
  "run_shell_command",
  "process",
  "read_file",
  "replace",
]

[tool_options.mouse_control]
enabled_coordinate_methods = ["manual", "ocr", "prediction"]
