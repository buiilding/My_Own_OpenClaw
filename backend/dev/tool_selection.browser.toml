# Browser-only tools profile
enabled = true
mode = "allowlist"
tools = [
  "browser",
  "run_shell_command",
]

[tool_options.mouse_control]
enabled_coordinate_methods = ["manual", "ocr", "prediction"]
