@"
feat: Complete LLM provider architecture refactor and parser simplification

- **LLM Provider Architecture**: Replaced string-based provider handling with object-oriented design
  - Created abstract LLMProvider base class with standardized interface
  - Implemented concrete providers: OpenAI, Gemini, Anthropic, OpenRouter, Mistral, Local (Ollama/LMStudio), Default
  - Simplified LiteLLMClient to act as thin orchestrator delegating to provider objects
  - Removed complex caching mechanism for simplicity

- **Response Parser Simplification**: Eliminated false-positive tool detection
  - Removed Gemma format and fallback text parsing (tool_name(param="value"))
  - Now exclusively supports Gemini CLI structured format: {"functionCall": {"name": "tool_name", "args": {...}}}
  - Prevents accidental tool execution from natural language explanations

- **Conversation History Optimization**: Improved multimodal message handling
  - Internal format now uses dictionaries with explicit "message" and "image_data" fields
  - Removed redundant _cached_llm_history for better memory efficiency
  - On-the-fly conversion to LLMMessage format when building prompts

- **Memory Integration Refinement**: Explicit context sections
  - Memory retrieval now triggered only for user messages (not tool outputs)
  - Updated to use explicit section headers: [Semantic Memory] and [Recent Interactions]
  - Aligned with system prompt memory formatting requirements

- **Tool Output Consistency**: Unified UI/LLM message display
  - UI now displays exact same formatted message as saved to LLM history
  - Removed double-iteration over tool results and redundant methods
  - Screenshots and OCR results properly integrated into single message

- **Streaming Architecture**: Normalized event handling
  - Updated to use standardized StreamingChunk types (content, thinking, toolCall, error)
  - Fixed event mapping for frontend compatibility
  - Resolved streaming bugs and "assistant not responding" issues

- **Code Cleanup**: Removed debug code and unused imports
  - Eliminated sensitive data logging in exception tracebacks
  - Cleaned up LLM client initialization logic
  - Removed legacy pattern matching and fallback strategies

BREAKING CHANGES:
- Tool calls now require strict JSON format only
- Provider configuration moved to dedicated classes
- Memory context format changed to explicit sections
- Streaming events use normalized chunk types
"@ | Set-Content commitmsg.txt
