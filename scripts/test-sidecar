#!/usr/bin/env bash
# Runs the sidecar workflow for the developer CLI and automation tooling.

set -euo pipefail

ROOT="$(cd "$(dirname "${BASH_SOURCE[0]}")/.." && pwd)"

cd "$ROOT"
"$ROOT/scripts/python-in-env" sidecar python -m pytest tests/sidecar "$@"
