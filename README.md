# Desktop Assistant

This is a personal desktop assistant designed to help users with various tasks through natural language interaction, leveraging large language models and a suite of tools for computer automation.

## Project Structure

- `backend/`: Python backend containing the core agent logic, memory system, tool marketplace, and voice processing.
- `frontend/`: Electron application providing the user interface.
- `tools/`: Directory for community-contributed marketplace tools.
- `docs/`: Project documentation, architecture, and developer guides.
- `tests/`: Unit and integration tests for both backend and frontend.

## Getting Started

### Prerequisites

- Python 3.9+
- Node.js (LTS) & npm
- Git

### Backend Setup

1.  Navigate to the `backend/` directory:
    ```bash
    cd backend
    ```
2.  Create and activate a Python virtual environment:
    ```bash
    python -m venv venv
    source venv/bin/activate  # On Windows, use `.\venv\Scripts\activate`
    ```
3.  Install Python dependencies:
    ```bash
    pip install -r requirements.txt
    ```

### Frontend Setup

1.  Navigate to the `frontend/` directory:
    ```bash
    cd frontend
    ```
2.  Install Node.js dependencies:
    ```bash
    npm install
    ```

### Running the Application

(Instructions to be added in a future issue)

## Development

(Details on linting, testing, and contribution to be added in future issues)
