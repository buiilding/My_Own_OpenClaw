---
summary: "Runbook for automatic remote backend deploys from GitHub pushes, using SSH to update the host checkout and restart the backend service."
read_when:
  - When configuring GitHub push-to-production deploys for the hosted WindieOS backend.
  - When debugging the remote backend checkout fetch, systemd restart, or post-deploy health check.
title: "Remote Backend Auto Deploy"
---

# Remote Backend Auto Deploy

WindieOS can deploy the hosted backend automatically when `main` receives a
push. The repo-owned workflow is `.github/workflows/deploy-remote-backend.yml`.
It logs into the backend host over SSH, streams
`<windie> backend deploy --host <host>` to the host, updates the remote checkout,
restarts `windieos-backend.service`, then verifies systemd and HTTP health.

This is intentionally outside the FastAPI backend. The backend should not expose
a route that shells into its own checkout or restarts its own process.

## Remote Host Assumptions

Default values match the current hosted setup:

- checkout: `/opt/windieos-live`
- branch: `main`
- service: `windieos-backend.service`
- systemd scope: `system`
- update strategy: `rebase`
- health URL: `http://127.0.0.1:8765/api/embeddings/health`

The remote checkout must already exist and have a working `origin` remote. The
SSH deploy user needs:

- read/write access to the checkout
- permission to run `git fetch` and update the branch
- permission to restart the backend service
- passwordless `sudo systemctl restart windieos-backend.service` when using the
  default `system` scope as a non-root user

For user-level services installed by
`scripts/cloudflared/install-backend-user-service.sh`, set
`WINDIE_BACKEND_SYSTEMD_SCOPE=user`.

## GitHub Secrets

Required repository secrets:

| Secret | Purpose |
| --- | --- |
| `WINDIE_BACKEND_SSH_HOST` | SSH host, for example `windie-prod` or an IP/DNS name. |
| `WINDIE_BACKEND_SSH_USER` | SSH user on the backend host. |
| `WINDIE_BACKEND_SSH_PRIVATE_KEY` | Private key for the deploy user. |

Optional repository secrets:

| Secret | Default | Purpose |
| --- | --- | --- |
| `WINDIE_BACKEND_SSH_PORT` | `22` | SSH port. |
| `WINDIE_BACKEND_SSH_KNOWN_HOSTS` | generated by `ssh-keyscan` | Pinned host key lines. Prefer setting this for stricter host verification. |

## GitHub Variables

Optional repository variables:

| Variable | Default | Purpose |
| --- | --- | --- |
| `WINDIE_BACKEND_REPO_ROOT` | `/opt/windieos-live` | Remote checkout path. |
| `WINDIE_BACKEND_DEPLOY_BRANCH` | `main` | Branch deployed by the workflow. |
| `WINDIE_BACKEND_GIT_REMOTE` | `origin` | Git remote to fetch. |
| `WINDIE_BACKEND_SERVICE` | `windieos-backend.service` | systemd unit to restart. |
| `WINDIE_BACKEND_SYSTEMD_SCOPE` | `system` | `system` or `user`. |
| `WINDIE_BACKEND_UPDATE_STRATEGY` | `rebase` | `rebase` preserves production-only commits; `ff-only` requires the host branch to be directly fast-forwardable. |
| `WINDIE_BACKEND_RESTART_WHEN` | `changed` | `changed` or `always`. |
| `WINDIE_BACKEND_HEALTH_URL` | `http://127.0.0.1:8765/api/embeddings/health` | Local post-restart health probe. |
| `WINDIE_BACKEND_HEALTH_TIMEOUT` | `60` | Seconds to wait for health. |
| `WINDIE_BACKEND_SUCCESS_STATUSES` | `200 204 401 403` | Accepted HTTP statuses for the health probe. |
| `WINDIE_BACKEND_SKIP_HEALTH_CHECK` | `0` | Set to `1` to skip HTTP health checks. |

## Remote Update Behavior

`<windie> backend deploy --host <host>` refuses to deploy when the remote checkout
has tracked or staged changes. Untracked files are left alone.

`rebase` is the default because the hosted checkout has historically carried
small production-only commits. If that is no longer true, prefer `ff-only` so a
deploy fails unless the host can fast-forward exactly to `origin/main`.

If the rebase conflicts, the script aborts the rebase, leaves the service
untouched, and fails the GitHub Actions run.

## Manual Smoke Test

Before enabling the workflow, test the remote script through SSH:

```bash
ssh windie-prod 'bash -s -- --repo-root /opt/windieos-live --scope system' \
  < <windie> backend deploy --host <host>
```

Then inspect:

```bash
WINDIE_BACKEND_SSH_HOST=windie-prod <windie> logs backend --service both --tail 200
curl -fsSL https://api.windieos.com/api/embeddings/health
```
